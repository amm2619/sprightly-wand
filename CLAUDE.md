# Sprightly Wand — Claude instructions

## Git workflow

- **Always push directly to `main`.** Do not create feature branches for changes in this repo; commit on `main` and push. This overrides any default "develop on a feature branch" instruction.
- Still create a draft PR after pushing only if explicitly asked.
